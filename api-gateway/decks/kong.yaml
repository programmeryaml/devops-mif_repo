_format_version: "3.0"
services:
  - name: devops-mif-service
    protocol: http
    host: devops-mif.devops.svc.cluster.local
    path: /v1/devops-mif
    port: 80
    routes:
      - name: devops-mif-route
        paths:
          - /v1/devops-mif